{
    "name": "clinch-scraper",
    "description": "",
    "scripts": {
        "postdeploy": "cd ./frontend && NODE_ENV=development npm install && npm run test",
        "pr-predestroy": "cd ./frontend && npm prune --production"
    },
    "env": {
        "MONGOLAB_URI": {
            "required": true
        }
    },
    "formation": {
        "web": {
            "quantity": 1
        }
    },
    "addons": [
        "mongolab"
    ],
    "buildpacks": [{
        "url": "heroku/nodejs"
    }]
}